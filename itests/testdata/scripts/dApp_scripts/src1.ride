{-# STDLIB_VERSION 7 #-}
{-# CONTENT_TYPE DAPP #-}
{-# SCRIPT_TYPE ACCOUNT #-}

@Callable(i)
func invokeNext(amount: Int, dApp2: String) = {
    let addr = addressFromStringValue(dApp2)
    strict r = addr.invoke("returnPayment", [], [AttachedPayment(unit, amount)])
    []
}